{
  "name": "appsc-current-affairs-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build && npm --prefix server install && npm --prefix server exec -- playwright install chromium",
    "start": "npm --prefix server run start",
    "seed": "npm --prefix server run seed"
  }
}
